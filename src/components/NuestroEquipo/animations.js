import gsap from "gsap";

export const startAnimation = ({ firstColumn, secondColumn, thirdColumn, fourthColumn }) => {
    // const tl = gsap.timeline({
    //     scrollTrigger: {
    //         markers: true,
    //         pin: true,
    //         scrub: 1,
    //         start: "top top",
    //     },
    // });
    // tl.to(firstColumn, { y: "0%" }, "<");
    // tl.to(secondColumn, { y: "0%" }, "<");
    // tl.to(thirdColumn, { y: "0%" }, "<");
    // tl.to(fourthColumn, { y: "0%" }, "<");
    // return tl;
};
